<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền của drawable -->
    <solid android:color="#00FFFFFF" />

    <!-- Độ bo góc -->
    <corners android:radius="5dp" />

    <!-- Viền -->
    <stroke
        android:width="3dp"
    android:color="@color/gray" />

</shape>
